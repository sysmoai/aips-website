# nixpacks config for api-server subdirectory
# Root-level nixpacks.toml is used when root directory = /
# Trigger rebuild v3 - using Dockerfile to bypass frozen-lockfile
[phases.setup]
nixPkgs = ["nodejs_22"]
aptPkgs = []
