# nixpacks config for api-server subdirectory
# Root-level nixpacks.toml is used when root directory = /
# Trigger rebuild v2 - all 81 pnpm.overrides now in package.json
[phases.setup]
nixPkgs = ["nodejs_22"]
aptPkgs = []
